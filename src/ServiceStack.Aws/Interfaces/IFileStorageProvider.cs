using System;
using System.Collections.Generic;

namespace ServiceStack.Aws.Interfaces
{
    public interface IFileStorageProvider
    {
        //void Download(FileMetaData thisProviderMeta, FileMetaData downloadToMeta);
        //void CopyTo(FileMetaData thisProviderMeta, IFileStorageProvider targetProvider, FileMetaData targetMeta);
        //void InjectTransportInfo(TransportInfo transportInfo);
        //Byte[] Get(FileMetaData fileData);
        //void Store(FileMetaData fileData);
        //void Store(FileMetaData sourceMeta, FileMetaData targetMeta);
        //void Delete(FileMetaData fileData);
        //void DeleteFolder(string folderName, Boolean recursive);
        //void CreateFolder(string folderName);
        //void Move(string source, string target);
        //Boolean Exists(FileMetaData fileData);

        //IEnumerable<string> ListFolder(string folderName, Boolean recursive = false);
    }
}